import Box from '@mui/material/Box';

/**
 * Simple component to add a sapce to bottom of each route
 * @returns Stateless component
 */
export default function BottomSpacer() {
    return (
        <Box my={1} />
    );
}